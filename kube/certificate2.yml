apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: librimem-certificate
  namespace: cert-manager
spec:
  secretName: letsencrypt-cluster-issuer-key
  dnsNames:
    - librimem.online
  issuerRef:
    name: letsencrypt-cluster-issuer
    kind: ClusterIssuer
